package src.ezVentory;

public class StorageManager extends Manager{
    public StorageManager(String firstName, String lastName, String id, String password, int accessType) {
        super(firstName, lastName, id, password, accessType);
    }
}
